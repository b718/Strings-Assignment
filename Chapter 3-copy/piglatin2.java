/*
 * String answer;
 * 
 * while (str.length() > 0) {
 *   int a = str.indexOf(" ");
 *   String word = str.substring(0,a);
 *   
 *   answer = answer + pigLatinOneWord(word);
 *   str = str.substring(a+1);
 * 
 * }
 * 
 * 
 * 
 */